"""
Define an attribute that only gets computed on access once, then the attribute
is cached and returned from the cache on every access including initialzing
additional instances of the parent class and of the subclasses.
"""
